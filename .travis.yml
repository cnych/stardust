language: go
go:
  - 1.9.x
  - 1.8.x
  - 1.7.x
  - tip
os:
  - linux
  - osx
matrix:
  allow_failures:
    - go: tip
  fast_finish: true
# install:
  # - go get github.com/magefile/mage
  # - mage vendor
script:
  - go run config/example/main.go
  # - mage hugoRace
  # - mage -v check
  # - ./hugo -s docs/
  # - ./hugo --renderToMemory -s docs/
# before_install:
  # gem install must be run with sudo on OSX
  # - sudo gem install asciidoctor | gem install asciidoctor
